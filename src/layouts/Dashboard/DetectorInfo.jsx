import React from 'react';
import {withRouter} from "react-router";

function DetectorInfo(props) {
    return (
        <div>This is Detector Info</div>
    );
}

export default withRouter(DetectorInfo);